{
  "name": "monitor-tool",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "monitor-tool": "./analyser.js"
  },
  "scripts": {
    "package": "rm -f ../MonitorTool.zip && zip -rq ../MonitorTool.zip analyser.js package.json README.md"
  }
}
