{
  "name": "code-stream-consumer",
  "version": "1.0.0",
  "description": "Express.js consumer for streaming code clone detection",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "package": "rm -f ../CodeStreamConsumer.zip && zip -rq ../CodeStreamConsumer.zip Dockerfile README.md package.json src"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
